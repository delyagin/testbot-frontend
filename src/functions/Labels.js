export function TestSuiteLabel(props) {
    return (
      <span>{props.row.title}</span>
    )
  }

export function ContactLabel(props) {
    return (
      <span>{props.row.name}</span>
    )
  }

  export function ProductLabel(props) {
    return (
      <span>{props.row.title}</span>
    )
  }
